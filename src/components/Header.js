import React from 'react';

const Header = (props) => (
        <div className="header">
            <div className="container">
                <h1 className="header__title">Indecision App</h1>
                <h2 className="header__subtitle">Let the computer choose your fate</h2>
            </div>
        </div>
);

export default Header;
